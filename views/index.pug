extends layout

block content
  h1 Books in my Library
  p
    a(class="button" href="/books/new") Create New Book
  table
    thead
      tr
        th Title
        th Author
        th Genre
        th Year
    tbody
    if(books.length === 0)
      h3 Start adding some books by clicking "Create New Book"!
    else
      each book in books 
        tr 
          td
            a(href="/books/" + book.id)= book.title 
          td= book.author 
          td= book.genre 
          td= book.year
